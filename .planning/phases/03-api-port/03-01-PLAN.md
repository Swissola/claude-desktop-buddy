---
phase: 03-api-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/compat.h
autonomous: true
requirements: [PORT-01, PORT-03]
must_haves:
  truths:
    - "compat.h compiles for BOTH boards with no reference to M5.Axp anywhere in the StickS3 translation unit"
    - "The StickC-Plus (#else) branch cuts LDO2+LDO3 on idle sleep and restores them on wake (preserves D-08 idle-power behavior)"
    - "compatBeep(freq,dur) routes a UI beep to M5.Speaker.tone on both boards"
    - "BUTTON_A_PIN / BUTTON_B_PIN are defined for both boards so main.cpp's light-sleep wake compiles"
  artifacts:
    - path: "src/compat.h"
      provides: "board-agnostic M5.Power power block, board-specific rail-sleep bodies, compatBeep, button-pin macros"
      contains: "compatBeep"
  key_links:
    - from: "src/compat.h #else branch"
      to: "M5.Power.Axp192.setLDO2/setLDO3"
      via: "compatRailSleep/compatRailWake"
      pattern: "M5\\.Power\\.Axp192\\.setLDO[23]"
    - from: "src/compat.h compatBeep"
      to: "M5.Speaker.tone"
      via: "inline shim"
      pattern: "M5\\.Speaker\\.tone"
---

<objective>
Amend `src/compat.h` so it compiles correctly for BOTH boards under M5Unified, fixing the latent RF-04 blocker (the current `#else` branch calls `M5.Axp.*`, which no longer exists on either board) and adding the three helpers the source swaps in Wave 2 depend on.

Purpose: This is the foundation. Every Wave-2 plan (main.cpp, stats/xfer/data, buddy/character) calls helpers defined here. It must land first.
Output: An amended `src/compat.h` with: (1) a board-agnostic `M5.Power` power block, (2) board-specific rail-cut idle-sleep bodies in the classic-ESP32 `#else` branch (D-08/RF-03), (3) a `compatBeep` M5.Beep→M5.Speaker shim (D-10), (4) `BUTTON_A_PIN`/`BUTTON_B_PIN` macros (NEW Blocker 2), (5) pre-emptive `MC_DATUM`/`TL_DATUM` insurance defines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-port/03-CONTEXT.md
@.planning/phases/03-api-port/03-RESEARCH.md
@src/compat.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse the power block to board-agnostic M5.Power + add board-specific rail-cut idle-sleep bodies</name>
  <files>src/compat.h</files>
  <read_first>
    - .planning/phases/03-api-port/03-RESEARCH.md (sections "RF-04 — The corrected compat.h power section" and "RF-03 — Preserving idle-sleep power"; the verbatim replacement power block is there — copy from it, do NOT re-derive)
    - .planning/phases/03-api-port/03-CONTEXT.md (D-08 rationale)
    - src/compat.h (current lines 104-148 — the broken power block being replaced)
  </read_first>
  <action>
    Replace the current `#if defined(BOARD_STICKS3) / #else` power block (compat.h lines ~104-148) with the board-AGNOSTIC block specified verbatim in RESEARCH.md §RF-04. The power/battery/brightness helpers (`compatBatVoltage`, `compatBatCurrent`, `compatVBusVoltage`, `compatBatteryPct`, `compatScreenBreath`, `compatBacklight`, `compatPowerBtnShort`, `compatPowerOff`, `compatEnableCoulomb`) become ONE definition each using `M5.Power.*` (per the verified API table in RESEARCH.md) — the `#else` branch's `M5.Axp.*` bodies are deleted (RF-04). The `_compatBrightness()` accessor moves out of the conditional to shared scope. Keep the float-volts shape by dividing the int-mV returns by 1000.0f (Pitfall 3) so existing call sites that `*1000` stay numerically correct.

    Then keep ONLY `compatRailSleep`/`compatRailWake` board-specific: under `#if defined(BOARD_STICKS3)` they are no-ops; under `#else` they call `M5.Power.Axp192.setLDO3(0); setLDO2(0)` (sleep) and `setLDO3(3000); setLDO2(3000)` (wake), replicating the old `M5.Axp.SetSleep()`/`WakeUpDisplayAfterLightSleep()` rail-cut per D-08/RF-03. `M5.Power.Axp192.*` MUST stay strictly inside the `#else` (classic-ESP32) branch — it is not a member on S3 builds (Pitfall 4). Annotate the 3000mV restore voltage as `[ASSUMED A1 — confirm on hardware]` in a comment. Leave the existing `#if`-gated onboard-LED block (compatLedInit/compatLedSet) intact below it.
  </action>
  <acceptance_criteria>
    - `grep -c 'M5\.Axp' src/compat.h` (excluding comment lines: `grep -v '^//' src/compat.h | grep -c 'M5\.Axp'`) returns 0 — no live M5.Axp references remain.
    - `grep -c 'M5\.Power\.Axp192' src/compat.h` returns >= 4 (setLDO2/setLDO3 in both sleep and wake), and every such line sits inside the `#else` (non-BOARD_STICKS3) branch.
    - Each of compatBatVoltage / compatBatCurrent / compatVBusVoltage / compatBatteryPct / compatScreenBreath / compatBacklight / compatPowerBtnShort / compatPowerOff / compatEnableCoulomb is defined exactly once (not duplicated across an #if/#else).
    - `compatRailSleep` and `compatRailWake` each have a BOARD_STICKS3 no-op body and an #else rail-cut body.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && C=$(grep -v '^[[:space:]]*//' src/compat.h | grep -c 'M5\.Axp'); A=$(grep -c 'M5\.Power\.Axp192' src/compat.h); test "$C" -eq 0 && test "$A" -ge 4 && echo PASS || echo "FAIL axp=$C axp192=$A"</automated>
  </verify>
  <done>The power block is a single board-agnostic definition on M5.Power; rail-sleep/wake bodies cut and restore LDO2/LDO3 in the classic-ESP32 branch only; no live M5.Axp references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Add compatBeep (D-10), button-pin macros (Blocker 2), and MC_DATUM/TL_DATUM insurance defines</name>
  <files>src/compat.h</files>
  <read_first>
    - .planning/phases/03-api-port/03-RESEARCH.md (sections "D-10 — M5.Beep → M5.Speaker shim", "NEW Blocker 2 — BUTTON_A_PIN / BUTTON_B_PIN undefined", and §RF-05 final paragraph on MC_DATUM/TL_DATUM)
    - .planning/PROJECT.md ("StickS3 I/O" — to confirm/refute StickS3 button GPIOs for O1)
    - src/compat.h (current end of file)
  </read_first>
  <action>
    Per D-10, add `static inline void compatBeep(uint16_t freq, uint16_t dur)` that calls `M5.Speaker.tone((float)freq, (uint32_t)dur)` — the verified 2-arg parity shim (RESEARCH.md §D-10). Board-safe; works on both the StickC-Plus buzzer and the StickS3 ES8311 path. The speaker is enabled by `M5.begin(cfg)` in plan 02 (D-09), so no `begin()` is needed here.

    Per NEW Blocker 2, define `BUTTON_A_PIN` and `BUTTON_B_PIN` macros (guarded with `#ifndef`). For the classic-ESP32 (`#else`) branch use the VERIFIED StickC-Plus values 37/39 (M5Unified.cpp registers GPIO_NUM_37/39). For the `#if defined(BOARD_STICKS3)` branch use the `[VERIFIED]` StickS3 values BUTTON_A_PIN=11 (BtnA) / BUTTON_B_PIN=12 (BtnB), with a comment noting O1 RESOLVED: confirmed from the installed M5Unified board-detection source — `M5Unified.cpp` `case board_t::board_M5StickS3` reads BtnA via `GPIO_NUM_11` and BtnB via `GPIO_NUM_12`. The old StickC-Plus 37/39 are classic-ESP32 ADC pins and are WRONG for the ESP32-S3; do NOT reuse them on the S3.

    Pre-empt RF-05's MEDIUM-confidence datum risk: add `#ifndef MC_DATUM` / `#define MC_DATUM middle_center` and `#ifndef TL_DATUM` / `#define TL_DATUM top_left` (cheap insurance against `MC_DATUM`/`TL_DATUM` being undeclared during the Wave-3 build). Do NOT touch the M5.Lcd alias or text-datum call sites — RF-05 confirms M5.Lcd is a zero-churn alias of M5.Display.
  </action>
  <acceptance_criteria>
    - `grep -c 'compatBeep' src/compat.h` >= 1 and the body references `M5.Speaker.tone`.
    - `grep -c 'define BUTTON_A_PIN' src/compat.h` >= 1 and `grep -c 'define BUTTON_B_PIN' src/compat.h` >= 1.
    - StickS3 button macros use the VERIFIED GPIO 11 (BtnA) / 12 (BtnB) and carry a `[VERIFIED]` / O1-RESOLVED comment (source: M5Unified.cpp `board_M5StickS3` button read).
    - `grep -c 'define MC_DATUM' src/compat.h` >= 1 and `grep -c 'define TL_DATUM' src/compat.h` >= 1.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q 'compatBeep' src/compat.h && grep -q 'M5\.Speaker\.tone' src/compat.h && grep -q 'define BUTTON_A_PIN' src/compat.h && grep -q 'define BUTTON_B_PIN' src/compat.h && grep -q 'define MC_DATUM' src/compat.h && echo PASS || echo FAIL</automated>
  </verify>
  <done>compat.h defines compatBeep (→M5.Speaker.tone), BUTTON_A_PIN/BUTTON_B_PIN for both boards, and MC_DATUM/TL_DATUM insurance defines; StickS3 button pins set to VERIFIED GPIO 11/12 per O1 (M5Unified board-detection source).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AXP192 power rails ↔ firmware | Rail-cut idle-sleep must restore rails on wake or the device appears bricked (dark screen) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-D1 | Denial of Service | compatRailSleep/compatRailWake (LDO2/LDO3 cut) | mitigate | Use the verified `M5.Power.Axp192.setLDOx` register-level rail-cut; restore at `[ASSUMED 3000mV]`; wake-after-idle confirmed by the D-08 hardware smoke test in plan 05 |
| T-03-I1 | Information disclosure | power/battery telemetry | accept | Local-only battery/voltage values; no external exposure introduced |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — all deps locked in Phase 1; supply-chain checkpoint not applicable |
</threat_model>

<verification>
- `pio` is NOT run in this plan (compat.h alone does not produce a binary); the real dual-env build is plan 05. Acceptance here is symbol-level (grep gates above).
- The collapsed power block, rail-sleep bodies, compatBeep, button macros, and datum defines all present per the acceptance criteria.
</verification>

<success_criteria>
- compat.h has zero live `M5.Axp` references; `M5.Power.Axp192` appears only in the `#else` branch.
- compatBeep, BUTTON_A_PIN, BUTTON_B_PIN, MC_DATUM, TL_DATUM all defined.
- Wave-2 plans can now call these helpers.
</success_criteria>

<output>
Create `.planning/phases/03-api-port/03-01-SUMMARY.md` when done.
</output>
