---
phase: 03-api-port
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/main.cpp
autonomous: true
requirements: [PORT-01]
must_haves:
  truths:
    - "main.cpp includes compat.h instead of <M5StickCPlus.h>"
    - "main.cpp contains no M5.Axp, no M5.Rtc, and no M5.Beep references"
    - "M5.begin(cfg) enables IMU + speaker (D-09); the StickC-Plus idle-sleep rail-cut + IMU-sleep behavior is preserved (D-08)"
    - "Motor LEDC code uses core-3.x ledcAttach/ledcWrite(pin,..) so it compiles (NEW Blocker 1); motor behavior unchanged, no chimes added"
  artifacts:
    - path: "src/main.cpp"
      provides: "fully ported primary translation unit using compat helpers + unified API"
      contains: "compat.h"
  key_links:
    - from: "src/main.cpp setup()"
      to: "M5.begin(cfg) with internal_spk + internal_imu"
      via: "D-09 config block"
      pattern: "M5\\.begin\\(cfg\\)"
    - from: "src/main.cpp idle-sleep path"
      to: "compatRailSleep/compatRailWake + M5.Imu.sleep/begin"
      via: "RF-03 power-preserving port"
      pattern: "compatRailSleep|M5\\.Imu\\.sleep"
---

<objective>
Port `src/main.cpp` (the primary translation unit, 40+ legacy call sites) onto compat helpers + the unified API per the RESEARCH.md per-call-site port map, satisfying PORT-01. Includes the two NEW compile blockers that live in main.cpp: core-3.x LEDC migration and the button-pin macros (resolved by plan 01).

Purpose: main.cpp is the hub; until its `M5.Axp`/`M5.Rtc`/`M5.Beep`/`ledcSetup` calls are swapped, neither env compiles.
Output: A fully ported `src/main.cpp`: include swap, unified `M5.begin(cfg)` (D-09), `M5.Beep`→`compatBeep` (D-10), all `M5.Axp.*`/`M5.Rtc.*` → compat helpers, core-3.x LEDC migration (Blocker 1), and the D-08/RF-03 idle-sleep path ported to keep current StickC-Plus power behavior.

This plan EXCLUDES Phase-4 work: it does NOT add event chimes, does NOT resolve the motor/speaker LEDC-channel collision (just makes the motor compile + keeps current behavior), and does NOT relabel the "vibrate" setting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-port/03-CONTEXT.md
@.planning/phases/03-api-port/03-RESEARCH.md
@src/compat.h
@src/main.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Include swap + unified M5.begin(cfg) (D-09) + core-3.x LEDC migration (Blocker 1)</name>
  <files>src/main.cpp</files>
  <read_first>
    - .planning/phases/03-api-port/03-RESEARCH.md (sections "D-09 — Unified M5.begin() config", "NEW Blocker 1 — LEDC API changed", and the main.cpp rows of the "Per-call-site port map" for lines 1, 1221-1229, 1297)
    - .planning/phases/03-api-port/03-CONTEXT.md (D-09 rationale; LEDC-collision is Phase 4)
    - src/main.cpp (lines 1, ~31-36, ~103, ~112, ~1221-1230, ~1297 — current init + motor code)
    - src/compat.h (compatBeep, compatLedInit, button macros from plan 01)
  </read_first>
  <action>
    Swap `#include <M5StickCPlus.h>` (line 1) → `#include "compat.h"`.

    Replace the `setup()` init (around lines 1221-1229) with the D-09 config block described in RESEARCH.md §D-09: build `auto cfg = M5.config();` set `cfg.internal_imu = true; cfg.internal_spk = true; cfg.internal_mic = false; cfg.clear_display = true;` then `M5.begin(cfg);`. DELETE `M5.Imu.Init()` (line ~1223) and `M5.Beep.begin()` (line ~1224) — both are now handled by cfg. Replace `pinMode(LED_PIN,OUTPUT); digitalWrite(LED_PIN,HIGH)` (~1226-1227) with `compatLedInit()`. DELETE `M5.Beep.update()` (~1297) — M5.Speaker plays asynchronously; do NOT add a busy-wait (Pitfall 6).

    Migrate the motor LEDC calls to core-3.x (Blocker 1): `ledcSetup(VIBRATE_CH,500,8); ledcAttachPin(VIBRATE_PIN,VIBRATE_CH)` (~1228-1229) → `ledcAttach(VIBRATE_PIN, 500, 8)`. Every `ledcWrite(VIBRATE_CH, x)` (lines ~103, ~112, ~1230) → `ledcWrite(VIBRATE_PIN, x)` (pin, not channel). `VIBRATE_CH` (line ~36) becomes unused — leave or remove, harmless. PRESERVE motor behavior; do NOT add chimes or touch the channel-collision design (Phase 4).
  </action>
  <acceptance_criteria>
    - `grep -c 'M5StickCPlus.h' src/main.cpp` returns 0; `grep -c 'compat.h' src/main.cpp` >= 1.
    - `grep -c 'M5.begin(cfg)' src/main.cpp` >= 1; `grep -c 'M5.Imu.Init' src/main.cpp` == 0.
    - `grep -c 'M5.Beep.begin\|M5.Beep.update' src/main.cpp` == 0 (begin/update removed here; the `M5.Beep.tone` swap is Task 2's gate).
    - `grep -c 'ledcSetup\|ledcAttachPin' src/main.cpp` == 0; `grep -c 'ledcAttach(' src/main.cpp` >= 1.
    - No remaining `ledcWrite(VIBRATE_CH' occurrence: `grep -c 'ledcWrite(VIBRATE_CH' src/main.cpp` == 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test $(grep -c 'M5StickCPlus.h' src/main.cpp) -eq 0 && test $(grep -c 'M5.begin(cfg)' src/main.cpp) -ge 1 && test $(grep -c 'ledcSetup\|ledcAttachPin' src/main.cpp) -eq 0 && test $(grep -c 'M5.Beep.begin\|M5.Beep.update' src/main.cpp) -eq 0 && echo PASS || echo FAIL</automated>
  </verify>
  <done>main.cpp includes compat.h, inits via M5.begin(cfg) with speaker+IMU, uses compatLedInit, and the motor uses core-3.x ledcAttach/ledcWrite(pin); no M5.Beep.begin/update, ledcSetup, or ledcAttachPin remain (M5.Beep.tone swap is Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Swap all M5.Axp.* / M5.Rtc.* / M5.Beep.tone call sites per the port map</name>
  <files>src/main.cpp</files>
  <read_first>
    - .planning/phases/03-api-port/03-RESEARCH.md ("Per-call-site port map" → src/main.cpp table; rows for lines 192, 208, 294, 529, 605, 628-630, 879-881, 912, 1290, 1321-1323, 1435, 1625, 1642, 1677)
    - src/compat.h (compatScreenBreath, compatBacklight, compatBeep, compatPowerOff, compatPowerBtnShort, compatVBusVoltage, compatBatVoltage, compatBatCurrent, compatChipTempC, compatRtcGetTime/Date, compatLedSet)
    - src/main.cpp (the listed lines)
  </read_first>
  <action>
    Apply the main.cpp port-map rows exactly (RESEARCH.md): `M5.Axp.ScreenBreath(x)` → `compatScreenBreath(x)` (lines 192, 1625); `M5.Axp.SetLDO2(true/false)` → `compatBacklight(true/false)` (208, 1435, 1642); `M5.Beep.tone(freq,dur)` → `compatBeep(freq,dur)` guarded by `if (settings().sound)` per the existing beep() body (294); `M5.Axp.PowerOff()` → `compatPowerOff()` (529); `M5.Axp.GetBtnPress() == 0x02` → `compatPowerBtnShort()` (605, 1677) and `!= 0x02` → `!compatPowerBtnShort()` (1290); `M5.Axp.GetVBusVoltage() > 4.0f` → `compatVBusVoltage() > 4.0f` (628); `M5.Rtc.GetTime(&_clkTm)` → `compatRtcGetTime(&_clkTm)` (629) and `M5.Rtc.GetDate(&_clkDt)` → `compatRtcGetDate(&_clkDt)` (630); `M5.Axp.GetBatVoltage/GetBatCurrent/GetVBusVoltage` → `compatBatVoltage()/compatBatCurrent()/compatVBusVoltage()` (879-881); `M5.Axp.GetTempInAXP192()` → `compatChipTempC()` (912); `digitalWrite(LED_PIN, LOW/HIGH)` → `compatLedSet(true/false)` (active-low handled inside compat) (1321, 1323). Leave all `M5.Lcd.*`, `MC_DATUM`/`TL_DATUM`, and `M5.Imu.getAccelData` sites UNCHANGED (RF-05 — zero churn).
  </action>
  <acceptance_criteria>
    - `grep -c 'M5.Axp' src/main.cpp` == 0 and `grep -c 'M5.Rtc' src/main.cpp` == 0 and `grep -c 'M5.Beep' src/main.cpp` == 0 (the line-294 `M5.Beep.tone`→`compatBeep` swap completes here).
    - `grep -c 'compatScreenBreath\|compatBacklight\|compatBeep\|compatPowerOff\|compatPowerBtnShort\|compatRtcGet\|compatBatVoltage\|compatChipTempC\|compatLedSet' src/main.cpp` >= 10.
    - `M5.Lcd` call sites still present (not migrated): `grep -c 'M5.Lcd' src/main.cpp` >= 1.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test $(grep -c 'M5.Axp' src/main.cpp) -eq 0 && test $(grep -c 'M5.Rtc' src/main.cpp) -eq 0 && test $(grep -c 'M5.Beep' src/main.cpp) -eq 0 && test $(grep -c 'M5.Lcd' src/main.cpp) -ge 1 && echo PASS || echo FAIL</automated>
  </verify>
  <done>Every M5.Axp/M5.Rtc/M5.Beep call site in main.cpp is replaced by the mapped compat helper; M5.Lcd/datum/IMU-read sites left untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Port the idle-sleep power path (RF-03 / D-08) — preserve StickC-Plus behavior</name>
  <files>src/main.cpp</files>
  <read_first>
    - .planning/phases/03-api-port/03-RESEARCH.md ("RF-03 — Preserving idle-sleep power" table, and port-map rows 243-248, 263, 273-274)
    - .planning/phases/03-api-port/03-CONTEXT.md (D-08 — preserve runtime behavior, not just compile)
    - src/main.cpp (idle-sleep path ~lines 243-280: imuSleep, idlePowerDown/idlePowerRestore)
  </read_first>
  <action>
    Replace the `imuSleep()` body (~243-248, currently a raw `Wire1` 0x68 reg-0x6B write) with a single `M5.Imu.sleep();` and remove the now-dead `Wire1` transaction entirely (Pitfall 5 — do NOT leave a half-edited Wire1 call). In the idle-power path: `M5.Axp.SetSleep()` (263) → `compatRailSleep()`; `M5.Axp.WakeUpDisplayAfterLightSleep()` (273) → `compatRailWake()`; `M5.Imu.Init()` on wake (274) → `M5.Imu.begin()`. Leave the generic ESP32 light-sleep machinery (`esp_light_sleep_start`, `gpio_wakeup_enable`, timer wake at ~279-280 using BUTTON_A_PIN/BUTTON_B_PIN now from compat.h) and the BLE advertising-off/on calls UNCHANGED — RF-03 confirms only the AXP + IMU bits move. This preserves the optimized idle-sleep power behavior (D-08); the wake-restore LDO voltage is `[ASSUMED A1]` and is flagged for the plan-05 hardware smoke test.
  </action>
  <acceptance_criteria>
    - `grep -c 'Wire1' src/main.cpp` == 0 (raw IMU I2C fully removed).
    - `grep -c 'M5.Imu.sleep' src/main.cpp` >= 1 and `grep -c 'M5.Imu.begin' src/main.cpp` >= 1.
    - `grep -c 'compatRailSleep\|compatRailWake' src/main.cpp` >= 2.
    - `grep -c 'esp_light_sleep' src/main.cpp` >= 1 (generic light-sleep machinery retained).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test $(grep -c 'Wire1' src/main.cpp) -eq 0 && test $(grep -c 'M5.Imu.sleep' src/main.cpp) -ge 1 && test $(grep -c 'compatRailSleep' src/main.cpp) -ge 1 && test $(grep -c 'compatRailWake' src/main.cpp) -ge 1 && echo PASS || echo FAIL</automated>
  </verify>
  <done>imuSleep uses M5.Imu.sleep(); idle down/restore use compatRailSleep/Wake + M5.Imu.begin(); generic light-sleep + advertising logic unchanged; no raw Wire1 remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| idle-sleep rail cut ↔ wake | Device must wake and restore display/IMU after light-sleep, or it appears bricked |
| BLE host ↔ device | Inbound host data path unchanged this plan (RTC seed handled in plan 03 data.h) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-D1 | Denial of Service | idle-sleep path (compatRailSleep/Wake, M5.Imu.sleep/begin, esp_light_sleep) | mitigate | Port AXP/IMU bits onto verified M5Unified equivalents; leave generic light-sleep + GPIO-wake intact; wake-after-idle + display-restore confirmed by plan-05 D-08 hardware smoke |
| T-03-02-T1 | Tampering | motor LEDC (GPIO26) | accept | Pin-based ledcAttach/ledcWrite preserves existing behavior; gated by settings().vibrate; no new external input |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase — deps locked Phase 1 |
</threat_model>

<verification>
- No `pio` build in this plan (the source set is incomplete until plans 03 + 04 also land); acceptance is the symbol-level grep gates. The dual-env zero-error build is plan 05.
- All three tasks edit only `src/main.cpp` (exclusive ownership — no Wave-2 file conflict).
</verification>

<success_criteria>
- main.cpp has zero `M5.Axp`, `M5.Rtc`, `M5.Beep`, `Wire1`, `ledcSetup`, `ledcAttachPin` occurrences.
- main.cpp includes compat.h, inits with M5.begin(cfg) (speaker+IMU), and the idle-sleep path preserves StickC-Plus rail-cut behavior.
- M5.Lcd / datum / IMU-read sites untouched (zero churn).
</success_criteria>

<output>
Create `.planning/phases/03-api-port/03-02-SUMMARY.md` when done.
</output>
