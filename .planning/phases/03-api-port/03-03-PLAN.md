---
phase: 03-api-port
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/stats.h
  - src/xfer.h
  - src/data.h
autonomous: true
requirements: [PORT-02]
must_haves:
  truths:
    - "stats.h, xfer.h, data.h contain no M5.Axp and no M5.Rtc references"
    - "stats.h battery path no longer depends on the AXP coulomb counter; battery % comes from compatBatteryPct() on both boards (O2 / D-04)"
    - "data.h seeds the wall clock via compatRtcSet (the bounds-clamped Phase-2 helper)"
  artifacts:
    - path: "src/stats.h"
      provides: "battery/VBus stats via compat power helpers"
      contains: "compat.h"
    - path: "src/data.h"
      provides: "host time-seed routed through compatRtcSet"
      contains: "compatRtcSet"
  key_links:
    - from: "src/stats.h batteryPct()"
      to: "compatBatteryPct() (M5.Power.getBatteryLevel)"
      via: "O2 coulomb-drop rewrite"
      pattern: "compatBatteryPct"
    - from: "src/data.h _applyJson time path"
      to: "compatRtcSet"
      via: "host {\"time\":[epoch,tz]} seed"
      pattern: "compatRtcSet"
---

<objective>
Port the direct legacy API calls RESEARCH found in `src/stats.h`, `src/xfer.h`, and `src/data.h` onto compat helpers (PORT-02). These three files were not in CONTEXT's original main.cpp/buddy list but contain live `M5.Axp.*` / `M5.Rtc.*` calls that block the build.

Purpose: stats.h is compiled into BOTH main.cpp and buddy.cpp, so its `M5.Axp.*` calls break both translation units until ported.
Output: stats.h battery path rewritten off the AXP coulomb counter (O2/D-04), xfer.h battery/VBus reads via compat helpers, data.h RTC-set routed through `compatRtcSet`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-port/03-CONTEXT.md
@.planning/phases/03-api-port/03-RESEARCH.md
@src/compat.h
@src/stats.h
@src/xfer.h
@src/data.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port stats.h — include + power swaps + coulomb-gauge drop (O2 / D-04 / D-08)</name>
  <files>src/stats.h</files>
  <read_first>
    - .planning/phases/03-api-port/03-RESEARCH.md ("Per-call-site port map" → src/stats.h table, and the "Coulomb-gauge note (D-04/D-08)" paragraph, and Open Question O2)
    - .planning/phases/03-api-port/03-CONTEXT.md (D-08 — battery-% behavior change needs a hardware sanity check)
    - src/stats.h (lines ~313-357: batteryInit/batteryTick/batteryPct + the M5.Axp.* calls)
  </read_first>
  <action>
    Add `#include "compat.h"` near the top of stats.h. Swap per the port map: `M5.Axp.EnableCoulombcounter()` (~314) → `compatEnableCoulomb()` (no-op); `M5.Axp.GetVBusVoltage() > 4.0f` (~324) → `compatVBusVoltage() > 4.0f`; `M5.Axp.GetBatVoltage() * 1000` (~325, 332) → `compatBatVoltage() * 1000`; `M5.Axp.GetBatCurrent()` (~326) → `compatBatCurrent()`.

    Resolve the coulomb gauge (O2 — RECOMMENDED minimal both-board path): M5Unified exposes no coulomb counter, so `M5.Axp.GetCoulombData()` (~341, 357) has no equivalent. Rewrite `batteryPct()` (~355) to `return compatBatteryPct();` and reduce `batteryTick()` (~339) and `batteryInit()` (~313) to no-ops (or retain `batteryFull()`/voltage-based helpers using the compat voltage wrappers, but stop persisting the coulomb baseline). This is a deliberate behavior change for the StickC Plus (loses coulomb-integrated accuracy in favor of `M5.Power.getBatteryLevel()`) — annotate it `[D-08 human_needed: battery-% sanity check on hardware]`. Do NOT re-implement a coulomb integrator (Don't-Hand-Roll) or add direct AXP192 register reads (out of scope unless hardware testing rejects getBatteryLevel — O2).
  </action>
  <acceptance_criteria>
    - `grep -c 'M5.Axp' src/stats.h` == 0.
    - `grep -c 'compat.h' src/stats.h` >= 1 and `grep -c 'compatBatteryPct\|compatBatVoltage\|compatVBusVoltage\|compatEnableCoulomb' src/stats.h` >= 3.
    - `grep -c 'GetCoulombData' src/stats.h` == 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test $(grep -c 'M5.Axp' src/stats.h) -eq 0 && test $(grep -c 'GetCoulombData' src/stats.h) -eq 0 && test $(grep -c 'compat.h' src/stats.h) -ge 1 && echo PASS || echo FAIL</automated>
  </verify>
  <done>stats.h includes compat.h, uses compat power helpers, and battery % comes from compatBatteryPct(); no coulomb-counter calls remain; behavior change flagged for D-08 hardware check.</done>
</task>

<task type="auto">
  <name>Task 2: Port xfer.h + data.h — include/power swaps + RTC-set via compatRtcSet</name>
  <files>src/xfer.h, src/data.h</files>
  <read_first>
    - .planning/phases/03-api-port/03-RESEARCH.md ("Per-call-site port map" → src/xfer.h and src/data.h tables)
    - src/xfer.h (line 75 include; lines ~115-117 battery/VBus reads)
    - src/data.h (lines ~84-85 M5.Rtc.SetTime/SetDate)
    - src/compat.h (compatBatVoltage/compatBatCurrent/compatVBusVoltage/compatRtcSet)
  </read_first>
  <action>
    xfer.h: swap `#include <M5StickCPlus.h>` (line 75) → `#include "compat.h"`. Then `M5.Axp.GetBatVoltage() * 1000` (~115) → `compatBatVoltage() * 1000`; `M5.Axp.GetBatCurrent()` (~116) → `compatBatCurrent()`; `M5.Axp.GetVBusVoltage() * 1000` (~117) → `compatVBusVoltage() * 1000`.

    data.h: replace `M5.Rtc.SetTime(&tm); M5.Rtc.SetDate(&dt);` (~84-85) with the single combined Phase-2 helper `compatRtcSet(&tm, &dt);`. Add `#include "compat.h"` at the top of data.h for safety even though it arrives transitively via xfer.h→stats.h. This is the host `{"time":[epoch,tz]}` seed path; `compatRtcSet` already bounds-clamps the month index (WR-01) — do not weaken that clamp.
  </action>
  <acceptance_criteria>
    - `grep -c 'M5StickCPlus.h' src/xfer.h` == 0; `grep -c 'M5.Axp' src/xfer.h` == 0; `grep -c 'compatBatVoltage\|compatBatCurrent\|compatVBusVoltage' src/xfer.h` >= 3.
    - `grep -c 'M5.Rtc' src/data.h` == 0; `grep -c 'compatRtcSet' src/data.h` >= 1; `grep -c 'compat.h' src/data.h` >= 1.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test $(grep -c 'M5.Axp' src/xfer.h) -eq 0 && test $(grep -c 'M5StickCPlus.h' src/xfer.h) -eq 0 && test $(grep -c 'M5.Rtc' src/data.h) -eq 0 && test $(grep -c 'compatRtcSet' src/data.h) -ge 1 && echo PASS || echo FAIL</automated>
  </verify>
  <done>xfer.h includes compat.h and reads battery/VBus via compat helpers; data.h seeds the clock via compatRtcSet; no M5.Axp/M5.Rtc/M5StickCPlus.h remain in either.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE host → device (`data.h::_applyJson`) | Untrusted-ish `{"time":[epoch,tz]}` from a bonded peer crosses into the system clock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-T1 | Tampering | data.h host time-seed → compatRtcSet | mitigate | Route through the existing compatRtcSet which clamps the month index to [0,11] (WR-01) preventing OOB array index; trust boundary identical to pre-port (bonded/encrypted peer) |
| T-03-03-I1 | Information disclosure | battery/VBus telemetry over BLE | accept | Pre-existing local telemetry; no new exposure; bonded peer only |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase — deps locked Phase 1 |
</threat_model>

<verification>
- No `pio` build here (incomplete source set until plan 04 also lands); acceptance is symbol-level grep gates. Dual-env zero-error build is plan 05.
- Files (stats.h, xfer.h, data.h) are exclusive to this plan — no Wave-2 overlap with plan 02 (main.cpp) or plan 04 (buddy/character/buddies).
</verification>

<success_criteria>
- stats.h / xfer.h / data.h have zero M5.Axp, M5.Rtc, M5StickCPlus.h references.
- Battery % flows through compatBatteryPct(); host time seed flows through compatRtcSet.
- Coulomb-gauge behavior change flagged for the D-08 hardware battery-% check.
</success_criteria>

<output>
Create `.planning/phases/03-api-port/03-03-SUMMARY.md` when done.
</output>
