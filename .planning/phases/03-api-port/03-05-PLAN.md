---
phase: 03-api-port
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - src/compat.h
  - src/main.cpp
autonomous: false
requirements: [PORT-03]
user_setup:
  - service: hardware
    why: "D-08 requires an on-device smoke test of the StickC Plus runtime behavior; the executor cannot flash a physical device"
    dashboard_config:
      - task: "Flash the m5stickc-plus build to a physical M5StickC Plus and run the 6-item smoke checklist"
        location: "Local USB-connected M5StickC Plus device"
must_haves:
  truths:
    - "pio run -e m5stickc-plus exits 0 (zero compile errors)"
    - "pio run -e m5stack-sticks3 exits 0 (zero compile errors)"
    - "Both envs build from the identical src/ tree (no per-env source forks)"
    - "A D-08 human_needed hardware-smoke checklist is recorded for the phase verifier to surface"
  artifacts:
    - path: ".planning/phases/03-api-port/03-05-SUMMARY.md"
      provides: "build evidence for both envs + the hardware smoke checklist with human_needed items"
      contains: "human_needed"
  key_links:
    - from: "dual-env build"
      to: "PORT-03 acceptance"
      via: "both pio run commands exit 0"
      pattern: "pio run"
---

<objective>
Verify the full port compiles zero-error on BOTH environments from one source tree (PORT-03), building `m5stickc-plus` FIRST (it exercises the AXP192 `#else` compat branch and has never fully compiled on this toolchain), then `m5stack-sticks3`. Iterate on any first-time-compile errors beyond the mapped ones (RESEARCH anticipates ~2-4: datum names, button-pin values, incidental legacy symbols). Then record the D-08 hardware-smoke checklist for the StickC Plus as a `human_needed` checkpoint the phase verifier surfaces.

Purpose: This is the phase's automated acceptance bar (dual-env compile) plus the D-08 on-device "done" criteria.
Output: Both `pio run` commands exit 0; a recorded 6-item hardware smoke checklist; any compat.h/main.cpp fix-ups needed to reach zero-error.

Scope EXCLUDES Phase-4 work: no event chimes, no motor/speaker collision resolution, no settings relabel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-port/03-CONTEXT.md
@.planning/phases/03-api-port/03-RESEARCH.md
@platformio.ini
@src/compat.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both envs zero-error (m5stickc-plus first, then m5stack-sticks3); iterate on residual errors</name>
  <files>src/compat.h, src/main.cpp</files>
  <read_first>
    - .planning/phases/03-api-port/03-RESEARCH.md ("Build / Verify reality" section, the Pitfalls list, RF-05 datum note, and the Assumptions Log)
    - platformio.ini (both env definitions; m5stickc-plus uses no_ota.csv, m5stack-sticks3 uses partitions_8mb.csv + -DBOARD_STICKS3)
    - src/compat.h (in case a residual fix is needed — e.g. MC_DATUM/TL_DATUM already pre-empted in plan 01, or a Pitfall-4 Axp192-branch escape)
  </read_first>
  <action>
    Build the StickC-Plus env FIRST: `pio run -e m5stickc-plus` (on Windows, `pio` or `~/.platformio/penv/Scripts/platformio.exe`). It exercises the `#else` AXP192 compat branch and `M5.Power.Axp192` calls and has never fully compiled on core 3.3.9 — expect the first real surfacing of any classic-ESP32-only gaps. Then build `pio run -e m5stack-sticks3`.

    Iterate to zero-error: fix → rebuild the failing env → rebuild the other (a fix for one can regress the other — e.g. an `M5.Power.Axp192` call escaping the `#else` branch breaks the S3 build, Pitfall 4). Anticipated residual fixes (apply minimally if they appear, do NOT pre-emptively churn): `MC_DATUM`/`TL_DATUM` undeclared → already pre-empted in plan 01, confirm the defines resolve; an `M5.Power.Axp192` reference reaching the S3 build → move it inside the `#else` branch; any incidental `M5StickCPlus`-only symbol the grep inventory already cleared. Keep all fixes confined to compat.h / main.cpp; do NOT fork the source per-env. Stop when both commands exit 0.
  </action>
  <acceptance_criteria>
    - `pio run -e m5stickc-plus` exits 0 (zero compile/link errors).
    - `pio run -e m5stack-sticks3` exits 0 (zero compile/link errors).
    - No source file under src/ includes <M5StickCPlus.h>; no live M5.Axp/M5.Rtc/M5.Beep/ledcSetup/ledcAttachPin/Wire1 references remain (regression guard across the whole port).
    - Both builds used the identical src/ tree (no `#ifdef`-forked duplicate source files introduced).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pio run -e m5stickc-plus && pio run -e m5stack-sticks3 && test $(grep -rl 'M5StickCPlus.h' src/ | wc -l) -eq 0 && echo BUILD_PASS || echo BUILD_FAIL</automated>
  </verify>
  <done>Both pio run commands exit 0 from the identical source; no legacy includes/symbols remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-08 hardware smoke test on the M5StickC Plus (human_needed)</name>
  <files>.planning/phases/03-api-port/03-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/03-api-port/03-RESEARCH.md ("Hardware verification (D-08 — human_needed, StickC Plus)" table and the Assumptions Log A1/A3)
    - .planning/phases/03-api-port/03-CONTEXT.md (D-08)
  </read_first>
  <action>
    This task cannot be automated — the executor cannot flash a physical device. Present the 6-item D-08 smoke checklist to the user, then record their results (pass / fail / not-yet-run) under a `human_needed` heading in 03-05-SUMMARY.md. Do NOT mark the phase fully done on hardware grounds without the user's input; the automated build bar (Task 1) is independent and already satisfies PORT-03.
  </action>
  <what-built>
    The full StickC-Plus firmware port is compiled (Task 1 green). Per D-08, "done" for this phase includes an on-device smoke test confirming the ported power/RTC/LED/beep/idle-sleep behavior matches the pre-port device. The executor cannot flash hardware, so this is a human-run checklist; record results in the SUMMARY as human_needed items for the phase verifier to surface.
  </what-built>
  <how-to-verify>
    Flash the m5stickc-plus build to a physical M5StickC Plus (`pio run -e m5stickc-plus -t upload`), then confirm each of the 6 items:
    1. Battery % — INFO/DEVICE page shows a sane % vs charger state (this is the coulomb→getBatteryLevel change, A3).
    2. Clock/RTC — sync time from the bonded host, confirm the clock face is correct.
    3. LED — trigger an attention state; the GPIO10 red LED pulses.
    4. Idle power draw — leave on battery, screen off, idle past the sleep timeout; drain comparable to pre-port (rail-cut + IMU-sleep ported).
    5. Display restore after idle sleep — press a button after idle sleep; screen comes back clean (validates the [ASSUMED 3000mV] LDO restore, A1).
    6. UI beep — button presses + settings `sound` ON → audible beep (compatBeep / M5.Speaker + D-09 speaker init).

    StickS3: if a device is available, smoke-test display + speaker beep + battery %; otherwise clean compile/link (Task 1) is the StickS3 acceptance per D-08.
  </how-to-verify>
  <acceptance_criteria>
    - The 6-item checklist is recorded in 03-05-SUMMARY.md, each marked pass / fail / not-yet-run, under a `human_needed` heading.
    - Any failure (esp. display-restore = wrong LDO voltage A1, or battery-% = A3) is logged with the observed symptom for follow-up.
  </acceptance_criteria>
  <verify>
    <human-check>The 6-item StickC-Plus smoke checklist is recorded in 03-05-SUMMARY.md under a human_needed heading with each item's status.</human-check>
  </verify>
  <done>The hardware smoke checklist is recorded with per-item status; failures (if any) logged with symptoms for follow-up.</done>
  <resume-signal>Type "approved" once the StickC Plus smoke checklist is run (or "defer" with a note if no device is available right now — the build bar is already met).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| idle-sleep rail cut ↔ wake (device) | Confirmed on real hardware here: device must wake + restore display after light-sleep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-D1 | Denial of Service | rail-cut idle-sleep on real device ([ASSUMED 3000mV] restore) | mitigate | Hardware smoke item 5 (display restore after idle sleep) directly verifies the device wakes cleanly; failure flags the LDO voltage for correction |
| T-03-05-T1 | Tampering | build toolchain / dependency resolution | accept | Deps locked + vetted in Phase 1; no new installs; builds from pinned platform 55.03.39 / core 3.3.9 |
| T-03-05-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase — not applicable |
</threat_model>

<verification>
- PORT-03 acceptance = both `pio run` commands exit 0 (Task 1 automated gate).
- D-08 acceptance = the 6-item StickC-Plus hardware smoke checklist recorded as human_needed (Task 2 checkpoint).
- Regression guard: zero M5StickCPlus.h / M5.Axp / M5.Rtc / M5.Beep / ledcSetup / Wire1 across src/.
</verification>

<success_criteria>
- `pio run -e m5stickc-plus` and `pio run -e m5stack-sticks3` both exit 0 from the identical source.
- The hardware smoke checklist is recorded with human_needed status for the phase verifier.
- No Phase-4 scope (chimes, motor/speaker collision, settings relabel) was introduced.
</success_criteria>

<output>
Create `.planning/phases/03-api-port/03-05-SUMMARY.md` when done (must contain the dual-env build evidence and the human_needed hardware smoke checklist).
</output>
