---
phase: 04-haptics-chimes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.cpp
  - src/stats.h
autonomous: true
requirements: [HAPT-01, HAPT-02, HAPT-03]

must_haves:
  truths:
    - "All chime + volume code is confined behind `#if defined(BOARD_STICKS3)`; chime output and the chime/volume settings entries exist ONLY on the StickS3, never on the StickC Plus (D-01)"
    - "On the StickS3, each of the five events (approve/deny/attention/celebrate/connect) plays a square-wave chiptune chime via M5.Speaker instead of driving the motor (HAPT-01, D-02)"
    - "approve and deny play clearly different pitches (E5 659Hz vs A3 220Hz) — the distinction the motor could not make (D-03)"
    - "When an event chime is active, the UI button beep is suppressed on the StickS3 so the two cues never stack (D-06)"
    - "The settings feedback toggle reads 'chime' on the StickS3 and stays 'vibrate' on the StickC Plus (D-07)"
    - "A StickS3-only 'volume' settings entry cycles speaker volume levels, persists across reboot, and drives M5.Speaker.setVolume (D-08)"
    - "The StickC Plus LEDC vibration-motor path (the #else branch of vibrateTick + the unguarded settingsItems/applySetting indices 0-11) is unchanged (HAPT-02)"
    - "pio run -e m5stack-sticks3 builds with zero errors with the board-conditional chime code in place"
  artifacts:
    - path: "src/main.cpp"
      provides: "Chime engine (square-wave buffer, per-event freq tables, _chimeSetFreqs dispatch, board-conditional vibrateTick output, beep() D-06 guard), board-conditional settings label + volume cycler"
      contains: "CHIME_WAV"
    - path: "src/stats.h"
      provides: "volIdx field in Settings struct + s_vol persistence in settingsLoad/settingsSave"
      contains: "volIdx"
  key_links:
    - from: "vibratePatternAmp / vibratePatternAmpArr"
      to: "_chimeSetFreqs(pat)"
      via: "#if defined(BOARD_STICKS3) call appended before return"
      pattern: "_chimeSetFreqs"
    - from: "vibrateTick ON step (StickS3)"
      to: "M5.Speaker.tone(freq, dur, CHIME_CH, true, CHIME_WAV, sizeof(CHIME_WAV))"
      via: "6-arg square-wave tone overload"
      pattern: "M5\\.Speaker\\.tone\\(.*CHIME_WAV"
    - from: "beep()"
      to: "compatBeep"
      via: "if (!_vibPat) guard on StickS3"
      pattern: "if \\(!_vibPat\\)"
    - from: "applySetting volume case + settingsLoad"
      to: "M5.Speaker.setVolume"
      via: "applyChimeVolume() helper"
      pattern: "setVolume"
---

<objective>
Replace the StickS3 haptic output with audible chiptune chimes while leaving the StickC Plus LEDC vibration-motor path byte-for-byte unchanged, then relabel the settings feedback toggle and add a StickS3-only speaker-volume cycler.

Purpose: HAPT-01 (events become distinct chimes on the StickS3 ES8311 speaker), HAPT-02 (motor path preserved by code-scope), HAPT-03 (settings relabel + UI-beep-vs-chime overlap resolved), and D-08 (user-configurable volume).
Output: Modified `src/main.cpp` (chime engine + settings) and `src/stats.h` (volume persistence). All chime/volume code lives behind `#if defined(BOARD_STICKS3)`; the StickC Plus build is unaffected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-haptics-chimes/04-RESEARCH.md
@.planning/phases/04-haptics-chimes/04-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Board-conditional chime engine in vibrateTick + D-06 beep guard</name>
  <files>src/main.cpp</files>
  <read_first>
    - src/main.cpp lines 31-112 (VIBRATE_PIN/CH, PAT_* arrays, _vib* state, vibratePatternAmp, vibratePatternAmpArr, vibrateTick) — the code being modified
    - src/main.cpp lines 288-290 (beep() helper) and 1222-1245 (setup(): M5.begin, ledcAttach, settingsLoad order)
    - src/compat.h lines 144-151 (compatBeep — 4-arg sine wrapper, unchanged this plan)
    - .planning/phases/04-haptics-chimes/04-RESEARCH.md "Chime Engine Design", "Modified vibrateTick", "D-06 UI-Beep vs Chime Coexistence", and "Common Pitfalls" 1-3,6,7 — exact constants, frequencies, scoping rules
    - .planning/phases/04-haptics-chimes/04-CONTEXT.md decisions D-02..D-06
  </read_first>
  <action>
    Add a StickS3-only chime block to src/main.cpp immediately after the VIBRATE_CH declaration (~line 34), guarded by `#if defined(BOARD_STICKS3)`: a 16-sample square-wave buffer `static const uint8_t CHIME_WAV[]` = eight 0 bytes then eight 255 bytes (50% duty, chiptune character per D-02 — NOT the library default sine); `static constexpr uint8_t CHIME_CH = 0`; and the Retro voice frequency tables per RESEARCH: `CHIME_APPROVE_FREQ = 659.0f` (E5), `CHIME_DENY_FREQ = 220.0f` (A3), `CHIME_ATTN_FREQS[] = {330,330,784}` (E4,E4,G5), `CHIME_CELEB_FREQS[] = {523,659,784,1047}` (C5,E5,G5,C6), `CHIME_CONN_FREQS[] = {392,523}` (G4,C5). Keep the layout theme-ready per D-04 (the constants are the Retro voice; a future voice adds parallel tables).

    Add StickS3-only state vars after the _vib* state block (~after line 59): `static float _vibFreq = 440.0f;` and `static const float* _vibFreqArr = nullptr;`. Add a StickS3-only `static void _chimeSetFreqs(const uint16_t* pat)` helper that sets _vibFreq/_vibFreqArr by pointer-equality dispatch on PAT_APPROVE/PAT_DENY/PAT_CONNECT/PAT_ATTENTION/PAT_CELEBRATE (440Hz fallback for unknown), per RESEARCH (pointer equality is reliable — PAT_* are file-scope static arrays). Place _chimeSetFreqs before vibratePatternAmp.

    In vibratePatternAmp and vibratePatternAmpArr, append `#if defined(BOARD_STICKS3)` `_chimeSetFreqs(pat);` `#endif` just before each function returns. Call sites stay unchanged (D-05 — reuse existing rhythm skeleton).

    Fork vibrateTick's output backend under `#if defined(BOARD_STICKS3)` / `#else`: pattern-end (pat[step]==0) → `M5.Speaker.stop(CHIME_CH)` vs `ledcWrite(VIBRATE_PIN, 0)`; ON step (step even) → `float freq = _vibFreqArr ? _vibFreqArr[_vibStep/2] : _vibFreq; M5.Speaker.tone(freq, _vibPat[_vibStep], CHIME_CH, true, CHIME_WAV, sizeof(CHIME_WAV));` vs the existing `uint8_t amp = _vibAmpArr ? _vibAmpArr[_vibStep/2] : _vibAmp; ledcWrite(VIBRATE_PIN, amp);`; OFF step (step odd) → `M5.Speaker.stop(CHIME_CH)` vs `ledcWrite(VIBRATE_PIN, 0)`. The `#else` branches MUST be the original ledcWrite lines verbatim (HAPT-02). CRITICAL (Pitfalls 2,3): every reference to CHIME_WAV/CHIME_CH/_vibFreq/_vibFreqArr must sit ONLY inside `#if defined(BOARD_STICKS3)` blocks so the StickC Plus translation unit never sees them.

    Modify beep() (~line 289) per D-06: under `#if defined(BOARD_STICKS3)` call `if (!_vibPat) compatBeep(freq, dur);` (suppress UI beep while a chime is active); `#else` keep `compatBeep(freq, dur);`. Use `_vibPat != nullptr` as the chime-active test, NOT M5.Speaker.isPlaying (Pitfall 6 — isPlaying is idle during rest gaps).

    Optionally guard the setup() motor init (~line 1232) with `#if !defined(BOARD_STICKS3)` around ledcAttach/ledcWrite (Pitfall 5 hygiene — GPIO26 unconnected on StickS3); the StickC Plus path inside the guard stays unchanged. Do NOT use any prohibited "v1/placeholder/static-for-now" reductions.
  </action>
  <verify>
    <automated>PATH="$HOME/.platformio/penv/Scripts:$PATH" pio run -e m5stack-sticks3</automated>
  </verify>
  <acceptance_criteria>
    - src/main.cpp contains `CHIME_WAV`, `CHIME_CH`, `_chimeSetFreqs`, and `M5.Speaker.tone(` with `CHIME_WAV` as an argument
    - src/main.cpp contains `CHIME_DENY_FREQ` = 220.0f and `CHIME_APPROVE_FREQ` = 659.0f
    - beep() contains `if (!_vibPat) compatBeep` inside a `#if defined(BOARD_STICKS3)` block
    - The `#else` branch of vibrateTick contains the original `ledcWrite(VIBRATE_PIN, amp)` and `ledcWrite(VIBRATE_PIN, 0)` calls unchanged
    - `pio run -e m5stack-sticks3` exits 0
  </acceptance_criteria>
  <done>StickS3 build is green; vibrateTick drives M5.Speaker square-wave tones on the StickS3 and the unchanged ledcWrite path on the StickC Plus; UI beep is suppressed while a chime plays.</done>
</task>

<task type="auto">
  <name>Task 2: Settings relabel (D-07) + StickS3 volume cycler (D-08)</name>
  <files>src/main.cpp, src/stats.h</files>
  <read_first>
    - src/main.cpp lines 317-357 (settingsItems[], SETTINGS_N, applySetting switch) — hardcoded absolute indices
    - src/main.cpp lines 437-499 (drawSettings — hardcoded `i ==` value-display branches incl. the stray `i == 13` brightness print)
    - src/main.cpp lines 1222-1245 (setup(): settingsLoad() at ~1236)
    - src/stats.h lines 180-242 (Settings struct, _settings initializer, settingsLoad, settingsSave)
    - .planning/phases/04-haptics-chimes/04-RESEARCH.md "D-07 Settings Label Relabel"
    - .planning/phases/04-haptics-chimes/04-CONTEXT.md decisions D-07, D-08
  </read_first>
  <action>
    D-07 relabel (HAPT-03): make settingsItems[] index 2 board-conditional — `"chime"` under `#if defined(BOARD_STICKS3)`, `"vibrate"` under `#else`. The internal field name `s.vibrate` and applySetting `case 2` stay identical; only the displayed label changes.

    D-08 volume entry (StickS3-only). INDEX BOOKKEEPING (critical — the menu diverges per board): insert `"volume"` into settingsItems[] on the StickS3 ONLY, positioned AFTER `"pet"` and BEFORE `"reset"` so shared indices 0-11 stay byte-identical on both boards and only the 3-item tail (volume/reset/back) diverges. Resulting StickS3 order: ... index 11 "pet", 12 "volume", 13 "reset", 14 "back"; StickC Plus order is unchanged (12 "reset", 13 "back"). Make SETTINGS_N board-conditional: 15 on StickS3, 14 on StickC Plus. Introduce board-aware index symbols (e.g. `IDX_VOLUME`/`IDX_RESET`/`IDX_BACK` via `#if defined(BOARD_STICKS3)`) and use them in applySetting and drawSettings for the tail so the divergence is explicit rather than scattered magic numbers.

    Settings struct + persistence (src/stats.h): add `uint8_t volIdx;` to the Settings struct (unconditional — a harmless 1-byte field keeps the struct identical across boards; only StickS3 reads it). Extend the `_settings` initializer with its default (index 2). In settingsLoad add `_settings.volIdx = _prefs.getUChar("s_vol", 2);` with a bounds clamp; in settingsSave add `_prefs.putUChar("s_vol", _settings.volIdx);`.

    Volume levels + apply (src/main.cpp, StickS3-only): define `static const uint8_t VOL_LEVELS[] = {0, 64, 128, 192, 255};` with parallel labels `{"mute","low","med","high","max"}` and a count constant; default volIdx 2 = 128 (the RESEARCH desk-companion recommendation). Add a helper `applyChimeVolume()` whose body under `#if defined(BOARD_STICKS3)` calls `M5.Speaker.setVolume(VOL_LEVELS[settings().volIdx]);` (no-op `#else`). Call applyChimeVolume() in setup() right after settingsLoad() (~line 1236) and in applySetting's volume case after cycling `s.volIdx = (s.volIdx + 1) % VOL_LEVELS_N;`. In applySetting, add the StickS3-only `case IDX_VOLUME` and re-point the reset/back cases to IDX_RESET/IDX_BACK so the existing reset (case currently 12) and back (currently 13) behaviors are preserved on both boards. In drawSettings, add a StickS3-only value branch that prints the VOL label for the volume row, and adjust the value-display index checks for the tail so the stray `i == 13` brightness print does NOT fire on the StickS3 reset row (guard or re-point it). Keep settingsSave() called at the end of applySetting so volume persists. No scope reductions.
  </action>
  <verify>
    <automated>PATH="$HOME/.platformio/penv/Scripts:$PATH" pio run -e m5stack-sticks3</automated>
  </verify>
  <acceptance_criteria>
    - settingsItems[] contains `"chime"` inside a `#if defined(BOARD_STICKS3)` block and `"vibrate"` inside the `#else`
    - settingsItems[] contains `"volume"` only within a BOARD_STICKS3 guard
    - SETTINGS_N resolves to 15 under BOARD_STICKS3 and 14 otherwise
    - src/stats.h Settings struct contains `volIdx`; settingsLoad reads `s_vol`; settingsSave writes `s_vol`
    - src/main.cpp contains `M5.Speaker.setVolume(` reached via applyChimeVolume(), called from both setup() and the volume applySetting case
    - applySetting reset/back behavior is preserved on the StickC Plus (indices 12/13 unchanged there)
    - In drawSettings, the stray `i == 13` brightness value-display branch is guarded (`#if !defined(BOARD_STICKS3)`) or re-pointed via the board-aware index symbols so it does NOT print the brightness value on the StickS3 reset row (index 13 on StickS3 = reset, not brightness)
    - `pio run -e m5stack-sticks3` exits 0
  </acceptance_criteria>
  <done>StickS3 menu shows "chime" + a working persisted "volume" cycler that drives M5.Speaker.setVolume; StickC Plus menu shows "vibrate" with identical indices/handlers; build is green.</done>
</task>

<task type="auto">
  <name>Task 3: Dual-env build gate + StickC Plus motor-branch diff check</name>
  <files>src/main.cpp</files>
  <read_first>
    - .planning/phases/04-haptics-chimes/04-CONTEXT.md decision D-09 (StickS3 primary; StickC Plus compile is non-gating)
    - .planning/ROADMAP.md "Phase 4" success criteria
  </read_first>
  <action>
    Verify the StickS3 env builds green (PRIMARY acceptance gate per D-09). Then run the StickC Plus env as a NON-BLOCKING sanity compile — if it fails, capture the error in the SUMMARY but do NOT treat it as a phase blocker (the device is repurposed; HAPT-02 is satisfied by code-scope). Finally, prove HAPT-02 by code-scope: run `git diff` on src/main.cpp and confirm every change to the vibrateTick/setup motor path is inside an added `#if defined(BOARD_STICKS3)` / `#else` seam and the `#else` (StickC Plus) lines are the original ledcWrite calls — i.e. the StickC Plus compiled output is unchanged. Record the diff confirmation in the SUMMARY.
  </action>
  <verify>
    <automated>PATH="$HOME/.platformio/penv/Scripts:$PATH" pio run -e m5stack-sticks3 && (PATH="$HOME/.platformio/penv/Scripts:$PATH" pio run -e m5stickc-plus || echo "STICKC-PLUS-NONBLOCKING-FAIL")</automated>
  </verify>
  <acceptance_criteria>
    - `pio run -e m5stack-sticks3` exits 0 (blocking)
    - StickC Plus build result recorded (green, or failure noted as non-blocking per D-09)
    - SUMMARY states that the StickC Plus motor `#else` branch in vibrateTick is the original ledcWrite code (HAPT-02 code-scope confirmation)
  </acceptance_criteria>
  <done>StickS3 builds green; StickC Plus build status recorded; HAPT-02 confirmed by diff inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 4 adds no network, input-parsing, auth, or crypto surface. The only change touching externally-influenced code is the beep() suppression guard, a pure boolean read of `_vibPat` which is set only internally by vibratePattern*. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | M5.Speaker audio output | accept | Audio chime is not a security-relevant sink; carries no PII or secret. No mitigation needed. |
| T-04-02 | Tampering | _vibPat chime-active flag read in beep() | accept | Flag is set only by internal vibratePattern* calls, never from BLE/serial inbound data; not attacker-influenced. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | N/A — Phase 4 installs no packages (RESEARCH "Package Legitimacy Audit": not applicable; M5Unified 0.2.17 locked in Phase 1). No supply-chain action this phase. |
</threat_model>

<verification>
- PRIMARY: `pio run -e m5stack-sticks3` exits 0 with the board-conditional chime + volume code present.
- HAPT-02: StickC Plus motor `#else` branch is the unchanged ledcWrite path (git diff confirmation).
- StickC Plus compile is a non-blocking sanity check (D-09).
- Hardware listening test is deferred to plan 04-02 (the on-device acceptance gate).
</verification>

<success_criteria>
- StickS3 build green with chime engine, D-06 beep guard, D-07 label, and D-08 volume cycler in place.
- All chime/volume symbols confined to `#if defined(BOARD_STICKS3)` blocks; StickC Plus path byte-for-byte in compiled output.
- Volume setting persists across reboot (s_vol NVS key) and drives M5.Speaker.setVolume.
</success_criteria>

<output>
Create `.planning/phases/04-haptics-chimes/04-01-SUMMARY.md` when done.
</output>
